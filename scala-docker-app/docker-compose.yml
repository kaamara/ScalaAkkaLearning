# Uruchomienie: docker compose up --build
# Strona: http://localhost:8080
#
# Zmienne ponizej maja wartosci domyslne, wiec plik .env nie jest wymagany.
# Jesli chcesz inne dane do bazy - skopiuj .env.example do .env i zmien.
#
# UWAGA: domyslne haslo jest widoczne w repo. Wystarczy do nauki na
# wlasnym komputerze, ale nie do niczego wystawionego na internet.
#
# Wartosci domyslne musza byc takie same w obu miejscach: baza dostaje je
# jako POSTGRES_*, aplikacja jako DB_*.
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-mojabaza}
    # Dane bazy trzymane poza kontenerem, zeby przetrwaly "down".
    # Kasuje je dopiero "down -v".
    # Uwaga: zmiana hasla dziala tylko na nowym wolumenie.
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      # Podwojny $$ - zmienna ma sie rozwinac w kontenerze, nie tutaj.
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M

  scala-app:
    build: .
    # Czeka, az baza bedzie gotowa.
    depends_on:
      db:
        condition: service_healthy
    # Te same wartosci domyslne co wyzej, tylko pod innymi nazwami.
    environment:
      DB_URL: jdbc:postgresql://db:5432/${POSTGRES_DB:-mojabaza}
      DB_USER: ${POSTGRES_USER:-app}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      HTTP_PORT: "${BACKEND_PORT:-8081}"
    # Port widoczny tylko wewnatrz sieci compose. Z zewnatrz wchodzi sie
    # przez nginx, wiec backendu nie da sie ominac.
    expose:
      - "${BACKEND_PORT:-8081}"
    healthcheck:
      # CMD-SHELL, zeby $$HTTP_PORT rozwinelo sie w kontenerze.
      # 127.0.0.1 zamiast localhost - na alpine localhost wskazuje najpierw
      # na IPv6, a serwer slucha tylko po IPv4.
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://127.0.0.1:$${HTTP_PORT}/ || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    # Bez limitu Java widzi cala pamiec komputera i bierze jej za duzo.
    deploy:
      resources:
        limits:
          memory: 512M

  frontend:
    image: nginx:alpine
    depends_on:
      scala-app:
        condition: service_healthy
    # Strona na http://localhost:8080 (port zmienisz przez FRONTEND_PORT).
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    # Nginx musi wiedziec, na ktory port backendu kierowac ruch.
    # FILTER ogranicza podmiane do zmiennych na BACKEND_, zeby envsubst
    # nie zjadl wlasnych zmiennych nginxa ($uri, $host...).
    environment:
      BACKEND_PORT: "${BACKEND_PORT:-8081}"
      NGINX_ENVSUBST_FILTER: "BACKEND_"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      # Pliki z /etc/nginx/templates obraz sam przepuszcza przez envsubst
      # i zapisuje wynik do /etc/nginx/conf.d.
      - ./nginx.conf.template:/etc/nginx/templates/default.conf.template:ro
    healthcheck:
      # Tu tez 127.0.0.1 - nginx slucha tylko po IPv4.
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
    restart: unless-stopped

volumes:
  postgres-data:
